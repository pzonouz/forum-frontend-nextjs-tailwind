const Posts = (props) => {
  const { className } = props;
  return (
    <div className={className}>
      <h2 className="font-bold text-lg text-center mt-4">آخرین مطالب وبلاگ</h2>
    </div>
  );
};
export default Posts;
